# Sources

Every factual claim in this repo about the exam, the Claude API, models, or pricing traces to an entry
here. Each entry carries a **verified on** date, because the credential is valid for only 12 months and
the exam guide is explicitly "subject to change without notice" — an undated claim rots silently.

**When you add a claim, add its source. When you re-check a source, update its date.**

## Primary — the exam

| Source | What it establishes | Verified |
|---|---|---|
| [Claude Certified Developer – Foundations Exam Guide (PDF)][guide] — v1.0, effective July 2026 | The whole of [`BLUEPRINT.md`](BLUEPRINT.md): 8 domains, 25 sub-skills, exact weights, 53 items, 120 min, 720/1000 cut score, $125 fee, 12-month validity, retake ladder, no prerequisites, scoring model, exam-day rules, NDA, renewal | 2026-09-04 |
| [Certification FAQ][faq] — Anthropic Partner Academy | Four certifications and fees; 720 cut score across all four; 120-min format; retake waits; 12-month validity; **registration requires a Partner Network organization email on a recognized company domain** | 2026-09-04 |
| [CCDV-F prep path][path] — Anthropic Partner Academy | The 5 prep modules and their durations; the 9 recommended prerequisite courses | 2026-09-04 |
| [Certification prep courses index][prep] | Which prep track maps to which credential | 2026-09-04 |

## Primary — the technology

Claims about API behavior, model tiers, Claude Code, Skills, and MCP should cite Anthropic's official
documentation rather than a community summary. Add rows as notes are written.

| Source | What it establishes | Verified |
|---|---|---|
| [Models overview][models] — Anthropic | The current lineup with model IDs, context windows, max output, and per-MTok pricing — Fable 5.1 at $10/$50 and 1M context, Opus 5 at $5/$25 and 1M, Sonnet 5 at $2/$10 and 1M, Haiku 4.5 at $1/$5 and 200K — with 128K max output on the first three and 64K on Haiku, and up to 300k output on the Batches API behind a beta header for several models; which models take adaptive versus extended thinking, that Fable 5.1's adaptive thinking is always on, that `high` is the default effort, and that Haiku 4.5 does not support effort at all; that every model ID is a pinned snapshot, including the dateless ones, while an alias before the 4.6 generation is a pointer to a dated ID; the selection guidance to start with Opus 5 for most workloads and reach for Fable 5.1 for demanding reasoning and long-horizon agentic work or when evals on Opus 5 at higher effort still fall short; the comparative latency ordering from Fable as slowest to Haiku as fastest; batch at 50% of synchronous price and cache reads at 10% of base input, or 2.5% on Fable 5.1; that a 1M context is roughly 555k words on the tokenizer introduced with Opus 4.7, where earlier models fit about 750k words in the same budget; the distinction between the reliable knowledge cutoff and the broader training-data cutoff; that the Models API reports `max_input_tokens`, `max_tokens`, and a capabilities object per model; and the published retirement commitments, which bind Anthropic-operated platforms while Amazon Bedrock and Google Cloud set their own dates | 2026-09-17 |
| [Steering thinking][steering] — Anthropic | The effort levels and what each does, with `high` as the default; that effort is set at `output_config.effort` rather than inside `thinking`; that changing effort mid-conversation invalidates the prompt cache while restating the default does not; that thinking counts toward `max_tokens` and there is no separate thinking budget; that billing covers the full reasoning regardless of the `display` setting; `usage.output_tokens_details.thinking_tokens` | 2026-09-05 |
| [How Claude remembers your project][memory] — Anthropic (Claude Code docs) | CLAUDE.md and auto memory are both loaded as context, not enforced configuration — "Claude treats them as context, not enforced configuration. To block an action regardless of what Claude decides, use a PreToolUse hook instead"; the CLAUDE.md scope table and its load order (managed policy, then user, then project, then local); that discovered files are concatenated into context rather than overriding each other, ordered from the filesystem root down to the working directory; that settings rules are enforced by the client regardless of what Claude decides, while CLAUDE.md instructions only shape behavior | 2026-09-08 |
| [Custom slash commands][slash] — Anthropic (Claude Code docs) | Personal commands live under `~/.claude/commands/` (or `~/.claude/skills/`) and are available on that machine across all projects; project commands live under `.claude/commands/` (or `.claude/skills/`) at the repo root, are shared with teammates via source control, and load for anyone who clones the repository; the command-name derivation from file/directory name; the scope priority order when names collide (enterprise, personal, project, nested, plugin, bundled) | 2026-09-08 |
| [Claude API errors][errors] — Anthropic | The HTTP error taxonomy (400 `invalid_request_error`, 401 `authentication_error`, 402 `billing_error`, 403 `permission_error`, 404 `not_found_error`, 409 `conflict_error`, 413 `request_too_large`, 429 `rate_limit_error`, 500 `api_error`, 504 `timeout_error`, 529 `overloaded_error`) as integration-layer failures, each with its own guidance — retry 500s with exponential backoff, resolve a 409 before retrying, and stream or batch a request that provoked a 504; that a stream can fail mid-response after an initial 200, which does not follow these HTTP-level mechanics; that errors are always JSON with an `error.type` and `error.message` plus a `request_id`, that the set of `type` values may grow under the versioning policy, and that callers should catch the SDKs' typed exception classes most specific first rather than string-matching a message; that every response carries a `request-id` header to quote to support, with a second AWS request ID on Claude Platform on AWS for CloudTrail; the per-endpoint request size limits (32 MB for Messages and token counting, 256 MB for batches, 500 MB for files) behind the 413; that the SDKs retry transient failures twice by default with exponential backoff, honoring `retry-after`; and that a non-streaming request is validated against a ten-minute timeout, so a long generation belongs on the streaming or batch path where polling replaces a held connection; and the common validation errors, each a 400 `invalid_request_error`: assistant prefill is not supported on Claude 4.6 and later, where structured outputs or system-prompt instruction replace it; `thinking` and `redacted_thinking` blocks in the latest assistant message cannot be edited, reordered, filtered, or reconstructed before being sent back, and with tool use every block must be replayed exactly as received, including one whose `thinking` field is empty; `thinking: enabled` is rejected by models that have removed extended thinking, and `thinking: adaptive` by models that predate it; and forced tool use through `tool_choice` `any` or `tool` is unsupported on some current models, where `auto` with strict tool use, or structured outputs, gives the shape instead | 2026-09-17 |
| [Messages API reference][messages-api] — Anthropic | That the Messages API is stateless, so each request is independent and prior turns must be resent in full; the `stop_reason` field and its values, in particular that `stop_reason: "max_tokens"` means the response was cut off because it hit the request's `max_tokens` limit and is therefore incomplete, distinct from `end_turn` (natural completion) or `stop_sequence` (custom stop string matched) | 2026-09-08 |
| [Self-hosted sandboxes][managed-self-hosted] — Anthropic | Managed Agents' two deployment models: the default Anthropic-hosted cloud sandbox runs tool execution, filesystem, and network egress on Anthropic's infrastructure, while self-hosted sandboxes move tool execution to the customer's own infrastructure while the model and orchestration stay on Anthropic's control plane | 2026-09-08 |
| [Agent SDK overview][agent-sdk-overview] — Anthropic (Claude Code docs) | What the Agent SDK is; its comparison table against the CLI, Client SDK, and Managed Agents — the SDK for building an agent without implementing the tool loop, the CLI for interactive terminal work, the Client SDK for direct API access where the caller implements the loop, and Managed Agents for long-running agents Anthropic hosts along with the sandbox; that it bundles the same agent loop, built-in tools, hooks, subagents, MCP, permissions, sessions, and `.claude/` configuration loading as Claude Code; and that the library exists for Python and TypeScript only, so driving the same agent loop from another language means running the CLI as a subprocess with `-p` and `--output-format json` | 2026-09-17 |
| [How the agent loop works][agent-loop] — Anthropic (Claude Code docs) | The turn and message lifecycle; that `max_turns` bounds tool-use turns independent of model behavior and yields an `error_max_turns` result subtype; the hooks table; that subagents start with a fresh context and return only a summary to the parent | 2026-09-08 |
| [Control execution with hooks][agent-hooks] — Anthropic (Claude Code docs) | Hooks as callbacks that run in the harness's own process to block dangerous operations before they execute, independent of what the model decided | 2026-09-08 |
| [Handle tool calls][handle-tool-calls] — Anthropic | That every `tool_use` block from one assistant turn must be answered by `tool_result` blocks in the single immediately following user message, each correlated by `tool_use_id`, with no messages in between | 2026-09-08 |
| [Context editing][context-editing] — Anthropic | The `clear_tool_uses` strategy selectively clears stale tool results in place once context passes a configured threshold, replacing each cleared result with placeholder text that tells Claude it was removed, and preserving the rest of the conversation — distinct from compaction, which summarizes and replaces the full history it covers; that clearing invalidates cached prompt prefixes, so `clear_at_least` sets a minimum number of tokens to clear and makes the invalidation worth paying for; and that the strategy suits agentic workflows with heavy tool use, where older file contents and search results are spent once Claude has read them | 2026-09-17 |
| [Building effective agents][effective-agents] — Anthropic | The workflow ("predefined code paths") versus agent ("dynamically directs its own process") definitions; that frameworks simplify low-level tasks but add abstraction that obscures debugging, and the recommendation to start with direct API use and the simplest solution | 2026-09-08 |
| [Subagents][sub-agents] — Anthropic (Claude Code docs) | That a subagent runs in its own separate context window and returns only a summary to the main conversation, keeping intermediate work out of the parent's context | 2026-09-08 |
| [Mitigate jailbreaks and prompt injections][mitigate-jailbreaks] — Anthropic | Direct versus indirect prompt injection threat models; JSON-encoding untrusted content with explicit delimiters so an attacker cannot break out into an instruction context; stating in the system prompt that delimited content is data, not commands; applying least privilege to limit what a successful injection can do; that pattern-based input screening is one layer among several, not a complete safeguard on its own | 2026-09-08 |
| [API key best practices][api-key-practices] — Anthropic | Resolving API keys from environment variables rather than hardcoding or committing them; that committing plaintext keys to a repository is a leading cause of key leaks; storing secrets in encrypted secret management in production rather than shared repository files | 2026-09-08 |
| [Python SDK][python-sdk] — Anthropic | Default automatic retries (connection errors, 408, 409, 429, and 5xx) with exponential backoff and a configurable `max_retries`; the default ten-minute timeout; the warning against a large `max_tokens` without streaming and the recommendation to stream long-running requests; the platform-specific client classes bundled in the base package | 2026-09-09 |
| [Claude in Amazon Bedrock][bedrock] — Anthropic | That Bedrock model IDs carry an `anthropic.` provider prefix and are reached through the platform's dedicated client class rather than a base-URL override of the first-party client | 2026-09-09 |
| [Prompting best practices][prompting-best-practices] — Anthropic | Setting a role in the system prompt to focus behavior and tone; using several diverse examples wrapped in `<example>` tags for a consistent output format; placing a long document above the query, instructions, and examples | 2026-09-09 |
| [Tool use with Claude][tool-use-overview] — Anthropic | That Claude selects a tool from the request and the tool's own description, so description scope and exclusions are what resolve selection ambiguity; the client-tool versus server-tool execution split; the built-in tool catalog | 2026-09-09 |
| [Agent Skills][skills-docs] — Anthropic (Claude Code docs) | What a Skill is — a loaded, prompt-based markdown bundle Claude orchestrates with its own tools — and how its scope and lifecycle differ from a custom tool or an MCP server | 2026-09-09 |
| [Structured outputs][structured-outputs] — Anthropic | That constrained decoding guarantees schema conformance only, not factual accuracy: a validly shaped payload carrying a high self-reported confidence can still be wrong, so validation and an explicit escalation flag are the trustworthy signals; the supported JSON Schema subset — basic types, `enum` of scalars, `const`, `anyOf`/`allOf`, internal `$ref`, `default`, `required`, and a fixed list of string formats — against the unsupported features: recursive schemas, external `$ref`, numerical constraints such as `minimum` and `maximum`, string constraints such as `minLength` and `maxLength`, and array constraints beyond `minItems` of 0 or 1; that every object must set `additionalProperties: false`; that an unsupported feature returns a 400 error; and that the SDKs strip unsupported constraints from the schema they send, add `additionalProperties: false`, and validate the response against the caller's original schema | 2026-09-17 |
| [Transports][mcp-transports] — Model Context Protocol specification (not Anthropic documentation) | stdio as newline-delimited messages over a client-launched subprocess's standard streams, versus Streamable HTTP as one networked endpoint any client can reach — the choice a shared deployment turns on | 2026-09-09 |
| [Resources][mcp-resources] — Model Context Protocol specification (not Anthropic documentation) | MCP resources as a standardized way for a server to expose contextual data to any connecting client, distinct from tools and from prompts | 2026-09-09 |
| [Prompt caching][prompt-caching] — Anthropic | Up to four explicit cache breakpoints per request, with a fifth returning a 400; that a cache write happens at the marked breakpoint as a prefix hash; that non-deterministic content such as a timestamp placed at or before a breakpoint silently prevents a cache hit with no error returned; that the cache lasts five minutes by default with a one-hour duration available at additional cost; the pricing shape — a five-minute cache write at 1.25 times base input, a one-hour write at 2 times, and a cache read at 0.1 times base input, lower still on some models — so a prefix read back only once costs more than not caching it; and that the minimum cacheable prompt length varies by model, from 512 to 4,096 tokens, below which a prompt marked with `cache_control` is processed without caching and no error is returned | 2026-09-17 |
| [API overview][api-overview] — Anthropic | The direct Claude API versus cloud-platform comparison (Amazon Bedrock, Google Cloud, Microsoft Foundry); that feature availability varies by platform rather than being automatically at parity; that cloud-platform authentication integrates with the provider's own identity system | 2026-09-09 |
| [Batch processing][batch-processing] — Anthropic | The Message Batches submit, poll, and retrieve lifecycle; that results can return in any order and must be matched to requests by `custom_id` rather than by position; the halved cost and the typical completion window of under an hour; that a batch is capped at 100,000 requests or 256 MB, whichever comes first; that results become accessible when every request finishes or after 24 hours, whichever comes first, and that a batch expires if processing does not complete within 24 hours, with per-request `expired` and `canceled` outcomes that are not billed; that results are downloadable as JSON Lines from the batch's results URL and remain available for 29 days after creation, after which the batch is still visible but its results are not; and that each batched request needs `max_tokens` of at least 1, so cache pre-warming with `max_tokens: 0` is unsupported inside a batch | 2026-09-17 |
| [Rate limits][rate-limits] — Anthropic | That Messages API limits are measured per model in requests, input tokens, and output tokens per minute, replenished continuously by a token bucket rather than reset on the hour, and reported through `anthropic-ratelimit-*` response headers; that exceeding one returns a 429 naming the limit with a `retry-after` header; that for most models `cache_read_input_tokens` do not count toward the input-token limit while `input_tokens` and `cache_creation_input_tokens` do, so caching raises effective throughput without raising the limit, and that `input_tokens` counts only what follows the last cache breakpoint; that output-token limits count tokens actually generated, so a higher `max_tokens` carries no rate-limit penalty; that reaching the tier's monthly spend cap also returns a 429 typed `rate_limit_error` but with no `retry-after` header and `error.details.error_code` of `enforced_spend_limit_reached`, which retries including the SDKs' own cannot clear; that a self-imposed spend limit returns a 400 instead; and that the Batches API carries its own request-rate and processing-queue limits shared across models | 2026-09-17 |
| [Commands reference][cc-commands] — Anthropic (Claude Code docs) | The built-in slash commands and their documented behavior: `/clear` starts a new conversation with empty context and is contrasted in the documentation itself with `/compact`, which frees context "by summarizing the conversation so far" while continuing the same conversation; `/resume` returns to an earlier conversation; `/init` initializes a project with a `CLAUDE.md` guide; `/permissions` manages allow, ask, and deny rules by scope; `/model` switches the model and saves it as the default for new sessions; `/cost` is an alias for `/usage`; `/help` shows help and available commands; that built-in commands execute fixed logic directly while bundled skills are prompt-based | 2026-09-17 |
| [Run Claude Code programmatically][cc-headless] — Anthropic (Claude Code docs) | Headless mode as `-p` / `--print` for non-interactive runs, exiting 0 on success and non-zero on failure so scripts can branch on status; that non-interactive mode reads standard input so data can be piped through it; the three `--output-format` values — `text` (default), `json` (structured, with the text in the `result` field), and `stream-json` (newline-delimited JSON, one event per line); that streaming needs `--verbose` and `--include-partial-messages`, that the last line of the stream is a `result` message carrying the final response text and session metadata, and that `system/init` is the first event reporting model, tools, and MCP servers; `--continue` for the most recent conversation and `--resume <session-id>` for a specific one; that `--bare` skips auto-discovery of hooks, skills, custom commands, subagents, plugins, MCP servers, auto memory, and `CLAUDE.md`, which is what makes a CI run reproducible across machines, and that without it a `-p` session runs a project's configured hooks and connects its `.mcp.json` servers even in a folder nobody has trusted, with no trust dialog and no per-server prompt; that bare mode reads no OAuth credential or keychain, so `ANTHROPIC_API_KEY` must be set; that piped stdin is capped at 10 MB, above which the run exits non-zero; and that a plugin or MCP server which fails to load does not fail the run — the `system/init` event's `plugin_errors` and `mcp_server_errors` arrays are what a CI gate inspects, since the run continues and exits cleanly; that `--permission-prompts none` denies anything that would prompt and continues rather than waiting on a host, while permission rules and the session's mode still decide each call first; and that at exit a background Bash task is terminated about five seconds after the final result, whereas a background subagent or workflow holds the process open until it finishes, bounded by a ten-minute idle wait that `CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS` configures, with SIGTERM exiting 143 and leaving the in-progress turn unfinished and unrecorded | 2026-09-17 |
| [Choose a permission mode][cc-permission-modes] — Anthropic (Claude Code docs) | Auto mode as the mode in which "a second model, the classifier, reviews actions instead of you", against Manual mode where Claude Code stops and asks before most actions that edit files, run shell commands, or reach the network; that `acceptEdits` writes files without prompting while other shell commands still need an allow rule; that `dontAsk` denies every call that would otherwise prompt | 2026-09-17 |
| [Hooks reference][cc-hooks] — Anthropic (Claude Code docs) | The exit-code contract a hook communicates through: exit 0 is success and the intended code when printing JSON for a structured decision, while exit 2 is a blocking error that blocks whether or not JSON is printed, so that even a JSON decision of `allow` cannot override it; that the effect of exit 2 depends on the event — it blocks the tool call on `PreToolUse`, blocks and erases the prompt on `UserPromptSubmit`, and cannot undo an action on `PostToolUse`, which runs after the tool has already run; that any other non-zero code does not block on its own for most events, so a policy must be enforced with exit 2; and that stderr from a hook exiting 0 reaches the debug log only and never the transcript or Claude, whereas on exit 2 the blocking message is the reason from the JSON decision, or the stderr text when there is none | 2026-09-17 |
| [Settings files and precedence][cc-settings] — Anthropic (Claude Code docs) | The five settings tiers and their order, highest first: managed settings deployed by the organization, the command line's `--settings`, project local `.claude/settings.local.json`, shared project `.claude/settings.json`, and user `~/.claude/settings.json`, where a key set at a higher tier overrides the same key set lower down; that nothing a user sets overrides managed settings apart from a few security-sensitive exceptions; that shared project settings are the tier meant for everyone in the project while project local is personal to one person in one project; and that Claude Code adds `**/.claude/settings.local.json` to the global git excludes the first time it writes that file, so a local settings file created by hand instead is not ignored until someone adds it to `.gitignore` | 2026-09-17 |
| [Create plugins][cc-plugins] — Anthropic (Claude Code docs) | The standalone-versus-plugin choice: a `.claude/` directory suits personal and project-specific customization, while a plugin suits sharing with teammates, versioned releases, and reuse across projects; that a plugin bundles skills, agents, hooks, MCP servers, LSP servers, background monitors, and default settings under a `.claude-plugin/plugin.json` manifest, with every directory but the manifest at the plugin root; that plugin skills are always namespaced as `/plugin-name:skill-name` to prevent collisions; that `--plugin-dir` loads a plugin without installing it and takes precedence over an installed plugin of the same name; that distribution is through marketplaces, including a private repository for a team; and, on migrating a standalone configuration, that project and user `.claude/agents/` definitions override a same-named plugin agent so the plugin version applies only once the original is removed, while a namespaced plugin skill coexists with the original rather than replacing it; and that a plugin may ship a `settings.json` at its root to apply default configuration when it is enabled, currently limited to the `agent` and `subagentStatusLine` keys, where `agent` activates one of the plugin's own agents as the main thread with its system prompt, tool restrictions, and model, those settings take priority over settings declared in `plugin.json`, and unknown keys are silently ignored | 2026-09-17 |
| [Discover and install plugins][cc-discover-plugins] — Anthropic (Claude Code docs) | That adding a marketplace registers a catalog without installing anything, and installing is a separate second step; the four installation scopes — user (yourself across all projects), project (all collaborators on the repository, written into `.claude/settings.json`), local (yourself in this repository only), and managed (administrator-installed and unmodifiable); that a team's `extraKnownMarketplaces` in project settings adds the marketplace once the member trusts the folder, but a plugin from an external source still does not load until that member installs it, with Claude Code reporting it as not installed and printing the install command; that removing a marketplace uninstalls the plugins installed from it; that disabling is reversible while uninstalling is not; and that plugins and marketplaces execute arbitrary code with the user's privileges, so only trusted sources should be added; that a plugin's details pane shows a context-cost estimate for the tokens it adds to the context window every turn, alongside the components it will install; and that Claude Code groups marketplace plugins unused for at least two weeks over at least ten sessions under a not-used-recently header with a last-used line, so plugins still paying startup and context cost without being used can be disabled or uninstalled; and that a community-marketplace plugin is pinned in the catalog to a specific commit, with continuous integration advancing the pin as the author pushes | 2026-09-17 |
| [Streaming messages][streaming] — Anthropic | That a response is streamed incrementally with server-sent events when `stream: true` is set; that the SDKs can stream internally and still return the complete `Message`, which is "especially useful for requests with large `max_tokens` values, where the SDKs require streaming to avoid HTTP timeouts"; that the API may send errors such as `overloaded_error` inside the event stream, where a non-streaming request would have returned HTTP 529; that an interrupted stream can be resumed from the point of interruption rather than re-requested in full | 2026-09-17 |

## Primary — this repository

A practice item may rest on this repository's own reference application, where it tests something
that application demonstrates rather than a claim about Anthropic's products. Such a citation is
recorded here on the same terms as an external page: what the file establishes, and the date it was
read. The file is the artifact, so re-reading it is re-running the check.

| Source | What it establishes | Verified |
|---|---|---|
| [`lab/output.py`][lab-output] — this repository | `TriageValidation.should_route_to_human` routes on validation status and the model's own `needs_human` flag, never on a self-reported confidence score alone; `is_valid` requires both a parsed payload and an empty error tuple, so a schema failure and a refusal both route to a person | 2026-09-17 |
| [`lab/config.py`][lab-config] — this repository | `PromptVersion` binds a system instruction to an explicit name and version rather than to a mutable default; `PROMPT_CATALOG` resolves an instruction by that pair; `compare_prompt_versions` orders two version strings component by component, padding the shorter, so a bump is comparable rather than merely different | 2026-09-17 |

## Deliberately not sources

- **Third-party practice-test vendors and exam-dump sites.** Not cited here at any point. They are
  frequently wrong, and the ones claiming real exam questions are trading in material every candidate
  is contractually barred from disclosing.
- **Other community study repos.** Useful for gauging what already exists; not evidence for a factual
  claim. If a claim only traces to another community repo, it is not sourced.

[guide]: https://everpath-course-content.s3-accelerate.amazonaws.com/instructor/6nizmqk8tpzpfjvt6qmmav7rh/public/1783542875/Claude+Certified+Developer+%E2%80%93+Foundations+Exam+Guide.pdf
[faq]: https://anthropic-partners.skilljar.com/page/faq-certifications
[path]: https://anthropic-partners.skilljar.com/path/claude-certified-developer-foundations
[prep]: https://anthropic-partners.skilljar.com/page/claude-certification-exam-prep-courses
[models]: https://platform.claude.com/docs/en/about-claude/models/overview
[steering]: https://platform.claude.com/docs/en/build-with-claude/thinking-steering-and-cost
[memory]: https://code.claude.com/docs/en/memory
[slash]: https://code.claude.com/docs/en/slash-commands
[errors]: https://platform.claude.com/docs/en/api/errors
[messages-api]: https://platform.claude.com/docs/en/api/messages
[managed-self-hosted]: https://platform.claude.com/docs/en/managed-agents/self-hosted-sandboxes
[agent-sdk-overview]: https://code.claude.com/docs/en/agent-sdk/overview
[agent-loop]: https://code.claude.com/docs/en/agent-sdk/agent-loop
[agent-hooks]: https://code.claude.com/docs/en/agent-sdk/hooks
[handle-tool-calls]: https://platform.claude.com/docs/en/agents-and-tools/tool-use/handle-tool-calls
[context-editing]: https://platform.claude.com/docs/en/build-with-claude/context-editing
[effective-agents]: https://www.anthropic.com/engineering/building-effective-agents
[sub-agents]: https://code.claude.com/docs/en/sub-agents
[mitigate-jailbreaks]: https://platform.claude.com/docs/en/test-and-evaluate/strengthen-guardrails/mitigate-jailbreaks
[api-key-practices]: https://support.claude.com/en/articles/9767949-api-key-best-practices-keeping-your-keys-safe-and-secure
[python-sdk]: https://platform.claude.com/docs/en/cli-sdks-libraries/sdks/python
[bedrock]: https://platform.claude.com/docs/en/build-with-claude/claude-in-amazon-bedrock
[prompting-best-practices]: https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices
[tool-use-overview]: https://platform.claude.com/docs/en/agents-and-tools/tool-use/overview
[skills-docs]: https://code.claude.com/docs/en/skills
[structured-outputs]: https://platform.claude.com/docs/en/build-with-claude/structured-outputs
[mcp-transports]: https://modelcontextprotocol.io/docs/concepts/transports
[mcp-resources]: https://modelcontextprotocol.io/docs/concepts/resources
[prompt-caching]: https://platform.claude.com/docs/en/build-with-claude/prompt-caching
[api-overview]: https://platform.claude.com/docs/en/api/overview
[batch-processing]: https://platform.claude.com/docs/en/build-with-claude/batch-processing
[rate-limits]: https://platform.claude.com/docs/en/api/rate-limits
[cc-commands]: https://code.claude.com/docs/en/commands
[cc-headless]: https://code.claude.com/docs/en/headless
[cc-permission-modes]: https://code.claude.com/docs/en/permission-modes
[cc-hooks]: https://code.claude.com/docs/en/hooks
[cc-settings]: https://code.claude.com/docs/en/settings
[cc-plugins]: https://code.claude.com/docs/en/plugins
[cc-discover-plugins]: https://code.claude.com/docs/en/discover-plugins
[streaming]: https://platform.claude.com/docs/en/build-with-claude/streaming
[lab-output]: https://github.com/moghazy17/ccdv-f-lab/blob/main/lab/output.py
[lab-config]: https://github.com/moghazy17/ccdv-f-lab/blob/main/lab/config.py
