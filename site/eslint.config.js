import js from "@eslint/js";
import astro from "eslint-plugin-astro";
import tseslint from "typescript-eslint";

export default [
  {
    ignores: [
      "node_modules/",
      ".astro/",
      "dist/",
      "build/",
      "coverage/",
      "*.min.js",
      "playwright-report/",
      "test-results/",
      // Vendored build output: the Pyodide runtime core and the runtime archive, neither authored
      // here and both regenerated by `npm run export:data`.
      "public/runtime/",
      // Temporary content copies the end-to-end propagation specs build against, left behind when a
      // run is interrupted. Nothing in them is authored here.
      ".us2-*/"
    ]
  },
  js.configs.recommended,
  ...tseslint.configs.recommended,
  ...astro.configs.recommended,
  {
    files: ["**/*.config.{js,mjs,ts}", "scripts/**/*.mjs"],
    languageOptions: {
      globals: {
        process: "readonly",
        URL: "readonly",
        console: "readonly",
        fetch: "readonly",
        Buffer: "readonly"
      }
    }
  }
];
